@model List<EntityLayer.Concrete.Message2>
@{
    ViewData["Title"] = "InBox";
    Layout = "~/Views/Shared/WriterLayout.cshtml";
}

<div class="page-header">
    <h3 class="page-title"> Gelen Kutusu </h3>
</div>
<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">
                    <a href="/Blog/BlogAdd" class="btn btn-sm btn-outline-info">Yeni Mesaj</a>
                </h4>
                <table class="table">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Konu</th>
                            <th>Gönderen</th>
                            <th>Mesajı Aç</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var msg in Model)
                        {
                            <tr>
                                <td>@msg.MessageID</td>
                                <td>@msg.Subject</td>
                                <td>@msg.SenderUser.WriterName</td>
                                <td>@(((DateTime)msg.MessageDate).ToString("dd-MMM-yyyy"))</td>
                                <td><a href="/Message/MessageDetails/@msg.MessageID" class="btn btn-sm btn-outline-warning">Aç</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

